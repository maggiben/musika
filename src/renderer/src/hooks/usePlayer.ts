import { useRef, useState, useCallback, useMemo, useEffect, DependencyList } from 'react';

const usePlayer = (deps?: DependencyList): void => {
  const waveSurferContainerRef = useRef<HTMLDivElement | null>(null);

  return;
};

export default usePlayer;
